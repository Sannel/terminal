cmake_minimum_required(VERSION 3.21)

# Phase 7: JSON settings model (profiles, color schemes, key bindings).
# Stub only — populated in Phase 7.
add_library(kterm-settings INTERFACE)
target_include_directories(kterm-settings INTERFACE include)
