cmake_minimum_required(VERSION 3.21)

# Phase 4: KTerminal class wiring VT parser → text buffer + input encoding.
# Stub only — populated in Phase 4.
add_library(kterm-core INTERFACE)
target_include_directories(kterm-core INTERFACE include)
